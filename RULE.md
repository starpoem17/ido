docs/personal 은 사용자가 수기로 작성한 내용이 들어있다. 절대 수정하지 않는다.

docs/plans 에는 계획을 세운다. docs/personal을 바탕으로 하여 구체적인 계획을 세운다.

docs/pseudo 에는 스도코드를 작성한다. docs/plans를 바탕으로 하여 실제 코드를 작성할 최대한 자세한 스도코드를 한국어로 작성한다.

파일에 접근할 때는 항상 personal-plans-pseudo-src의 위계질서를 지키며 상위 파일의 지침을 중시한다. 접근 시에도 상위 파일에 먼저 접근한다.

사용자의 허락없이 코드 작성을 시작하지 않는다.

accelerate 라이브러리로 bf16을 활용하여 모델 훈련

코드 작성 시에는 cpu 8코어 물리 코어를 적극 활용하도록 멀티프로세싱 코드를 작성, 혹은 단일 GPU를 최대한 활용할 수 있도록 병렬 연산 친화적으로 코드를 작성한다.
멀티프로세싱 코드 작성 시에는 프로세스를 7개 띄우도록 한다.
단, 사용할 라이브러리의 특성을 고려하여 멀티스레딩 지원이 되는 라이브러리의 경우에는 단일 프로세스로 진행하고 그렇지 않은 경우에만 멀티프로세싱 코드를 작성한다. 라이브러리 공식 문서를 확인한 뒤 사용자에게 보고하여 멀티프로세싱 코드를 작성할지 여부를 묻고 진행한다.

CPU와 GPU 메모리 이동을 최소화하여 메모리 오버헤드를 줄이는 코드를 작성한다.
LLM 모델 학습 코드 작성 시 pytorch 내장 flash attn을 강제 활성화하도록 코드를 작성한다. 풀백을 허용하지 않는다. fail fast를 적용한다. 단, 번역 및 데이터 증강 등 외부 모델 가중치를 불러와 실행하는 경우 flash attn 실패 시 일반 어텐션 연산을 적용하도록 한다.

코드 작성 시 향후 수정 가능성이 있는 변수들은 터미널에서 인자를 받아 적용하는 방식이 아닌 코드 맨 앞에 둬서 코드에서의 수정이 용이하도록 한다.
uv run python -m ...py 형태 명령어로 실행할 수 있도록 한다.
tqdm과 디버깅 로그 터미널 출력을 적용해 코드 진행 상황을 터미널에서 확인할 수 있도록 한다. tqdm은 tqdm(range(100), file=sys.stdout)와 같이 sys.stdout을 적용하여 사용한다.

사용자의 PC 환경
    cpu: amd 9800x3d 8코어
    gpu: rtx5090 32gb. 단일 GPU
    torch_cuda_arch_list=12.0
    dram: 32gb + nvme ssd swap 64gb
    os: ubuntu 24.04
    torch: 2.10.0
    cuda: 13.0
    nvidia driver: 580.95.05
    cudnn: 9.18.0
    uv로 파이썬 라이브러리 관리